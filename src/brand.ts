/**
 * PLACEHOLDER branding. "Northwind" is sample-database filler, not a real
 * tenant — replace name, monogram and the colour tokens in styles/theme.css
 * once the real logo and palette arrive.
 */
export const brand = {
  name: 'Northwind',
  suffix: 'People',
  monogram: 'N',
} as const
